import React from 'react'
import MarkCompleteTask from '../../../features/component/completemark/MarkCompleteTask';

const ReviewTable = ({task_complete, complete_length, incomplete_length, tasksectionStyle, code, taskscompletetableStyle, doughnutStyle, DrawDoughnut, doughnutPosition, task_issue_length,
    task_not_started_length, task_progress_length}) => {
  return (
<></>
  )
}

export default ReviewTable